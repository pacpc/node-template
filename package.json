{
  "name": "{{ name }}",
  "version": "{{ name }}",
  "description": "{{ name }}",
  "license": "{{ name }}",
  "author": "{{author + ' <' + email + '> ' + '(' + github + ')'}}",
  "main": "index.js",
  "scripts": {},
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=12"
  }
}
